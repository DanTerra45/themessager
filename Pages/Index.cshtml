@page
@model IndexModel
@{
    ViewData["Title"] = "Dashboard";
}

<section class="d-flex flex-column justify-content-center align-items-center" style="min-height: 50vh;">
    <h1 style="font-size: 5rem; margin-bottom: 1rem; text-transform: uppercase; font-family: var(--font-display); font-weight: 800; text-align: center; color: var(--mrc-ink); letter-spacing: -0.05em;">Bienvenido</h1>
    <p style="font-size: 1.5rem; font-family: var(--font-mono); text-align: center; color: var(--mrc-ink-light);">¿Qué vamos a hacer hoy?</p>
</section>