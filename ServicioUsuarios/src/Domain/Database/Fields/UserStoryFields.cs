namespace Domain.Database.Fields
{
    public enum UserStoryFields
    {
        Id,
        UserId,
        OperatorId,
        PreviousState,
        NewState,
        DisableReason,
        CreatedAt
    }
}